# rs-modde — Remaining Work (handoff context)

Companion to [TODO.md](TODO.md). The TODO file is a flat checklist; this
document gives each unticked item enough context for someone (or the next
agent) to pick it up cold without re-deriving the design.

Items are grouped by **how far they are from "ready to write code"** —
*Tractable* (clear path, just type it), *Needs design* (decisions first),
*Blocked* (waiting on prerequisite work).

---

## Tractable next steps

### Coverage baseline & badge (§3.1)
- **What's done:** `cargo-llvm-cov` is in the devShell, `just coverage*`
  recipes are live, Forgejo Actions runs `just coverage-ci` with
  `FAIL_UNDER=0`.
- **What's left:** Run coverage once locally (`just coverage-ci`), read
  the line-coverage %, then bump `FAIL_UNDER` in
  [.forgejo/workflows/ci.yml](.forgejo/workflows/ci.yml) to ~5 percentage points
  below that. For the badge: lcov already lands at
  `target/llvm-cov/lcov.info`; pick a host (codecov, coveralls, or a
  static SVG generated by `cargo-llvm-cov report --json` and uploaded
  to Forgejo releases) and add the upload step.

### Manifest parser proptest (§2.1)
- **What's there to model after:**
  [crates/modde-core/tests/resolver_proptest.rs](crates/modde-core/tests/resolver_proptest.rs).
- **Target:** `WabbajackManifest` round-trip — generate a minimal
  manifest, serialize, parse, assert equality. The trickiest property
  is **path normalization** (`\\` ↔ `/`, case folding). Look at
  `crates/modde-core/src/manifest/wabbajack.rs` for the parser surface
  and existing fixtures in [3077.wabbajack](3077.wabbajack).

### Concurrent VFS deploy stress test (§2.1, §1.2)
- **Why:** the symlink farm uses `tokio::fs` ops; current tests are
  serial. Real deploy under heavy load (e.g. 50k files, multiple
  profiles concurrently calling `materialize` against overlapping
  staging dirs) is unverified.
- **Shape:** spawn N tokio tasks calling
  `SymlinkFarm::build → materialize → deploy_to` against distinct
  tempdirs, then a second pass with **deliberately overlapping**
  staging dirs to surface races. Use `proptest`-generated mod-file
  layouts so the input space isn't fixed.
- **Failure-injection:** wrap `tokio::fs::symlink` calls behind a
  trait so tests can inject `EEXIST` / `ENOSPC` and assert recovery.

### CLI install/update happy paths (§2.4)
- **What's done:** failure-path coverage in
  [crates/modde-cli/tests/cli_install_mod.rs](crates/modde-cli/tests/cli_install_mod.rs)
  + the wiremock harness pattern.
- **`modde install mod` happy path** — needs:
  1. Mock `/games/{domain}/mods/{id}.json`, `/users/validate.json`
     (premium=true), `/games/.../files/{fid}/download_link.json`
     returning a CDN URL pointing back at the same wiremock instance.
  2. Mock the CDN URL to return the bytes of a fixture zip (one
     `.esp` + readme is plenty).
  3. Pre-create a profile with `modde profile create` so the post-
     download "add to profile" step has a target.
  4. Assert: profile now has the mod, store has the extracted
     directory, deploy is reachable via `modde deploy --dry-run`.
- **`modde update apply` happy path** — needs the install happy path
  first to seed the DB with `nexus_mod_id`-tagged rows. Then mock
  `/games/{domain}/mods/updated.json` to advertise a newer file_id and
  assert the row is rewritten. **Sharp edge:** `update apply` runs
  interactive `y/N` prompts on breaking semver; pass `--yes
  --accept-breaking` to bypass.

### Collision-detection bench (§2.6)
- **What to bench:** `ConflictMap::register` + `resolved_conflicts`
  in [crates/modde-core/src/resolver/mod.rs](crates/modde-core/src/resolver/mod.rs).
- **Pattern to follow:** copy
  [crates/modde-core/benches/vfs_deploy.rs](crates/modde-core/benches/vfs_deploy.rs)
  scaffolding. Inputs: N mods × M files with X% overlap on path names
  (parameterize the overlap ratio — that's the dial that exercises the
  conflict path).

### Archive-extract bench (§2.6)
- Targets: the native `modde-sources::decompress` readers. Three
  formats: zip (have fixtures), 7z (need a small fixture), BSA/BA2
  (Bethesda).
- Build small synthetic archives at bench-setup time so the repo
  stays small. Criterion's `iter_with_setup` keeps the archive
  creation outside the timed region.

### Resumable Wabbajack apply/import (§1.4)
- **What's done:** the install-pipeline rework landed resumable
  apply/import semantics for Wabbajack archive store entries; existing
  entries survive process restarts and are hash-verified before trust.
  See [docs/INSTALL_PIPELINE_REWORK.md](docs/INSTALL_PIPELINE_REWORK.md)
  Phase 9.

### `modde scan` UI button (§1.6)
- Add an entry in the actions panel (see `crates/modde-ui/src/app.rs`
  near the existing `Detect` / `Deploy` buttons). Hooks into the
  same `commands::scan::handle` the CLI uses.

---

## Needs design / discussion before coding

### Multi-instance flag wiring (§1.1)
- TODO bullet: "`--instance` flag wired through every command."
- **Open question:** today instance switching uses
  `modde instance switch <name>` which writes the active instance to
  a registry file, then *all subsequent commands* read from it. A
  `--instance` flag would override per-invocation, but every command
  would need to honour it before any path-resolving code runs.
  Options:
  1. **Process-wide override** (cheap): add `--instance` next to
     `--data-dir` in `Cli`, call `instance::set_active(name)` at
     startup. Wires through automatically because everything reads
     `paths::modde_data_dir()`.
  2. **Per-command parameter** (correct but invasive): every handler
     takes an `instance: Option<&str>` argument. Cleaner separation
     but ~30 handler signatures change.
- Recommendation: option 1, then audit hot commands to confirm no
  cached state escapes the override.

### MO2-portable instance subcommands (§1.1)
- TODO calls for `instance create / clone / delete / list`. `create`
  and `list` exist; `clone` and `delete` don't. **Clone semantics**
  need a decision:
  - Hard copy of the data dir? (Simple, but multi-GB.)
  - Reflink/copy-on-write where the FS supports it (btrfs/xfs/apfs)?
  - Symlink the immutable parts (store/) and copy the mutable parts
    (profiles, db)?
- Recommend the third — store is content-addressed and safe to share
  across instances.

### `GenericGame` config-driven definition (§1.2)
- The trait exists in `modde-games` (used by
  `cyberpunk`, `gamebryo`, etc. via the registry). Exposing it
  user-facing means a TOML/JSON spec in
  `~/.local/share/modde/games/<id>.toml` that gets registered at
  startup alongside the built-ins.
- **Risk:** plugin behaviours like `BethesdaGame::plugins_txt_folder`
  are hand-written Rust. A pure-data game can't replicate complex
  per-game logic — only the surface things (display name, install
  detection paths, scan directories). Decide what's expressible
  declaratively vs. what stays Rust-only.

### Merged-VFS browser (§1.2)
- Three sub-items: archive content visibility, hidden-file filters,
  origin tagging. All read-only views over the existing
  `SymlinkFarm.links` map plus the `installed_mod_files` table.
- **Hidden-file filters** are partially there — `SymlinkFarm::build`
  already takes `hidden: Option<&HashSet<(String, String)>>`. The
  missing piece is a UI/CLI surface to *toggle* hidden state and
  persist it (probably a `hidden_files` table keyed by
  `(profile_id, mod_id, rel_path)`).
- **Origin tagging:** the resolver's `ConflictMap` already knows
  every provider for every path. Surface that in a `modde tree
  --profile <name>` command.

### Mod-info dialog tabs (§1.5)
- Four TODO items, one dialog. The current dialog is a single-pane
  view in `crates/modde-ui/src/mod_info.rs` (or similar). Expanding
  to tabs (file tree, image preview, conflicts, metadata/Nexus) is
  mostly Iced wiring once the backing data is fetched.
- **Conflicts tab** is the highest-value tab — it surfaces the
  resolver's `resolved_conflicts` output for that mod, which today
  is only visible via `modde collisions`. Implement that first.
- **Image preview** needs the existing `NexusApi::fetch_bytes` (used
  by browse tiles) plus an Iced image widget — straightforward.

### "Problems" diagnostics panel (§1.5)
- Two TODO items: detection panel + guided fixes. Detection is what
  `modde diagnostics` already produces; the panel just displays it.
  **Guided fixes** are the harder part — each diagnostic needs a
  registered "remediation action" callback. Consider a
  `Diagnostic::auto_fix(&mut Profile) -> Result<()>` method on the
  diagnostic enum.

---

## Blocked / waits on other work

- **§1.4 BAIN installer user-input flow** — depends on §1.3's BAIN
  selection UI; one needs the other's hooks.
- **§2.2 plugin tests for BG3 / Stardew / FNV / Oblivion** — depends
  on §1.3 per-game implementations being feature-complete.
- **§2.3 BAIN end-to-end** — same chain as above.
- **§2.5 mod-info headless tests** — depends on §1.5 dialog landing.
- **§3.3 Fuzz targets** — `cargo-fuzz` needs nightly Rust; the
  toolchain in `flake.nix` is stable. Either: (a) gate fuzz under a
  `nightly` feature with a separate devShell, or (b) use
  `arbitrary`-based property tests that run on stable.

---

## What landed this session (for context)

- Coverage tooling: cargo-llvm-cov in devShell, `just coverage*`
  recipes, Forgejo Actions wired (FAIL_UNDER=0 placeholder).
- CLI integration harness: shared
  [tests/common/mod.rs](crates/modde-cli/tests/common/mod.rs)
  fixture isolating `MODDE_DATA_DIR`/HOME/XDG; insta snapshot tests
  for help output; end-to-end tests for nxm dispatch, nexus status
  (premium/free/401 via wiremock), install-mod failure paths,
  update-check short-circuit, scan dispatch, exec round-trip.
- modde-sources Nexus refactor: hardcoded API URLs replaced with
  env-var-aware `base_url()` / `graphql_url()` helpers
  (`MODDE_NEXUS_BASE_URL`, `MODDE_NEXUS_GRAPHQL_URL`) so the test
  harness can point the client at a wiremock server.
- `modde exec` top-level CLI alias: thin shim over the existing
  `modde tool *-executable` handlers; same DB row, more
  discoverable surface.
- Criterion + proptest workspace deps; `vfs_deploy` bench
  (build at 1k–50k files; materialize+deploy at 1k/10k files);
  resolver proptest with 4 properties.
- Install pipeline rework:
  [docs/INSTALL_PIPELINE_REWORK.md](docs/INSTALL_PIPELINE_REWORK.md)
  now records the landed per-archive batching, native decompression,
  streaming I/O, inline zip index, hardlink/reflink deploy, bounded
  byte cache, zstd staging, verify-on-download, and resumable apply
  work. CAS chunk dedup remains deferred there as Phase 7b.

Workspace state at session end: **1,570 tests passing**, zero
failures.

---

## How to find what's where

| Area | Entry point |
|------|-------------|
| Test fixtures | [crates/modde-cli/tests/common/mod.rs](crates/modde-cli/tests/common/mod.rs) |
| Wiremock pattern | [crates/modde-cli/tests/cli_nexus_status.rs](crates/modde-cli/tests/cli_nexus_status.rs) |
| Bench scaffolding | [crates/modde-core/benches/vfs_deploy.rs](crates/modde-core/benches/vfs_deploy.rs) |
| Proptest scaffolding | [crates/modde-core/tests/resolver_proptest.rs](crates/modde-core/tests/resolver_proptest.rs) |
| Coverage recipes | [justfile](justfile) (search `coverage`) |
| Nexus base-URL override | [crates/modde-sources/src/nexus/mod.rs](crates/modde-sources/src/nexus/mod.rs) (`base_url`, `graphql_url`) |
| Mod scanner design | [SCANNER_DESIGN.md](SCANNER_DESIGN.md) |
