# modde presentation website — Zola (custom templates)
# Local dev: cd website && zola serve
# Build: nix build .#website

base_url = "https://modde.rs"
title = "modde"
description = "NixOS-native game mod manager"
default_language = "en"

compile_sass = true
build_search_index = false
minify_html = false
generate_feeds = false
hard_link_static = true

[markdown]
[markdown.highlighting]
theme = "nord"
